# Security Policy

CanonLoom 默认只处理本地文件，不需要网络服务或 API key。使用时仍应注意：

- 不要把 `.env`、API key、账号信息或私有素材提交到 Git；
- 不要把未授权的参考作品全文放入公开仓库；
- 检查运行日志、绝对路径和上下文包是否包含隐私信息；
- 只给 Agent 授予必要的项目目录权限；
- 在公开发布前审阅 `canon/`、`memory/`、`runs/` 和 `logs/`。

如果发现脚本会越过作者批准、写入项目根目录之外，或泄露敏感数据，请不要公开利用问题；请通过私下渠道报告，并附上最小复现步骤。
